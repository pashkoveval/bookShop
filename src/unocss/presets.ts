import { Preset } from 'unocss'

export const defaultPreset: Preset = {
  name: 'defaultPreset',
  shortcuts: [
    {
      btnIcon: 'py-2 px-4 display-inline-flex cursor-pointer color-pp',
    },
  ]
}